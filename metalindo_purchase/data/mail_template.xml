<odoo>
    <data noupdate="1">
        <record id="purchase_regret_letter" model="mail.template">
            <field name="name">Purchase Order: Regret Letter</field>
            <field name="model_id" ref="purchase.model_purchase_order" />
            <field name="subject">${object.company_id.name} Regret Letter (Ref ${object.name or 'n/a' })</field>
            <field name="partner_to">${object.partner_id.id}</field>
            <field name="body_html" type="html">
                <div style="margin: 0px; padding: 0px;">
                    <p style="margin: 0px; padding: 0px; font-size: 13px;">
                        Dear ${object.partner_id.name}
            % if object.partner_id.parent_id:
                (${object.partner_id.parent_id.name})
            % endif
                        <br />
                        <br />
                        We regret to inform you that the quotation
                        <strong>${object.name}</strong>
                        has been rejected to be purchase order.
            If you have any questions, please do not hesitate to contact us.
                        <br />
                        <br />
                        Best regards,
                    </p>
                </div>
            </field>
            <field name="lang">${object.partner_id.lang}</field>
            <!-- <field name="user_signature" eval="False" /> -->
            <field name="auto_delete" eval="True" />
        </record>
    </data>
</odoo>